# betguard-assistant 專案規則

本專案是下注文字解析與風控助理，不是自動下注系統。

## 最高安全原則（不可妥協）

- 不確定就停。
- 不得自動修正使用者下注內容。
- 不得自動送出下注。
- 不得新增任何網站操作、自動化瀏覽器、Playwright 或 OCR 流程，除非使用者明確要求進入後續階段。
- Parser 只能解析明確格式；模糊格式應回報錯誤。
- Validator 必須阻擋重複號碼、超出範圍號碼、未知玩法與未知彩種。

## 第一階段範圍

- 文字解析
- 標準 JSON 輸出
- 風控檢查
- CLI
- 單元測試

## 工程原則

- 不做無謂的相容層；但已出貨的資料契約、API 與快取必須版本化遷移，不得直接移除。
- 選擇最簡且完整滿足需求的實作；避免臆測性抽象、設定與間接層。
- 分層成長：先做出最小可用的端到端版本，再在運作中的產品上疊加能力；不以未完成的複雜度換掉可用產品。
- 模組化、關注點分離。
- 優先使用成熟、維護良好的函式庫；不無故重造輪子。
- 先檢查專案既有依賴與文件，再決定是否新增套件。
- 架構決策以長期為準，不接受「之後再換」的暫時方案。

## 程式碼要求

- 使用 Python 3.11+。
- 使用 pytest。
- 資料模型需清楚且可測試。
- 新增功能前必須補測試。
- 錯誤訊息要保守、明確，不猜測使用者意圖。

## 禁止事項

- 禁止儲存下注帳號、密碼或網站登入資訊。
- 禁止加入自動點擊、自動送出、自動確認下注功能。
- 禁止把錯誤輸入修正成看似可下注的內容。
- 禁止在未知規則下假設某彩種或玩法可用。
